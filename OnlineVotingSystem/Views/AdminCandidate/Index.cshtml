@model IEnumerable<OnlineVotingSystem.Models.Candidate>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h2>Candidates</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>

@using (Ajax.BeginForm("Search", "AdminCandidate", new AjaxOptions
{
    HttpMethod = "GET",
    UpdateTargetId = "SearchResults",
    InsertionMode = InsertionMode.Replace
}))
{
<input type="text" name="q" />
    <select name="Filter">
    <option>Candidate ID</option>
    <option>Society</option>

    </select>
    <input type="submit" value="Search" />
}

<br />

@Html.Partial("_CandidatesDisplay", Model)
@*<table>
    <tr>
    <th></th>        
    <th>
        Candidate ID
        </th>
        <th>
            Society
        </th>
        <th>
            Manifesto
        </th>
        <th>
            Votes
        </th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink("Edit", "Edit", new { CandidateId=item.CandidateId }) |
            @Html.ActionLink("Details", "Details", new { CandidateId=item.CandidateId }) |
            @Html.ActionLink("Delete", "Delete", new { CandidateId=item.CandidateId })
        </td>
        <td>@item.CandidateId</td>
        <td>
            @item.Society
        </td>
        <td>
            @item.Manifesto
        </td>
        <td>
            @item.Votes
        </td>
    </tr>
}

</table>*@

